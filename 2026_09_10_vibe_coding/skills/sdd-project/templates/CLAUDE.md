# הוראות ל-Claude Code — {{PROJECT_NAME}}

הכללים המלאים נמצאים ב-`AGENTS.md`, שהוא הקובץ המשותף לכל הסוכנים בכל כלי.
Claude Code טוען אותו כאן אוטומטית:

@AGENTS.md

אם השורה שמעל לא נטענה — פתח את `AGENTS.md` וקרא אותו במלואו לפני כל פעולה.

## תזכורת קצרה

1. לפני: `progress/status.md` + `docs/journal/00-tracking.md` + `issues/` +
   `docs/journal/discussions.md`, ורשום את עצמך
2. תוך כדי: קובץ אחד, עורך אחד. מחוץ לתחום — issue, לא תיקון.
   **אין קוד בלי spec מאושר ותוכנית מאושרת**
3. לפני סיום: יומן + סשן + `progress/status.md` + סגירת issues + שחרור קבצים

**מה שלא כתוב במסמך — לא קרה.**
